# ProductOS content repo
node_modules/
.DS_Store

# Astro / Starlight site build
dist/
.astro/
npm-debug.log*

# src/content/docs/ publishes the repo's own markdown directly: the
# top-level entries there are symlinks into the repo root (guides/, anchors/,
# templates/, examples/, skills/, pm-playbook/, AGENTS.md) plus the
# hand-written homepage (index.mdx). All of those are tracked; nothing under
# src/content/docs/ is generated anymore.
